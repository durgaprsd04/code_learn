using System;
using System.IO;
using System.Linq;
using System.Collections;
using System.Collections.Generic;
namespace LeetCode
{
    class ConstructBinaryFromPreOrder
    {
      public TreeNode BuildTree(int[] preorder, int[] inorder)
      {
        List<int> preIndex = new List<int>();
        List<int> inIndex = new List<int>();
        //var root = new TreeNode(preorder[0], null,null);
        //root.left = new TreeNode(preorder[1],null,null);
        //var rightIndex =GetIndex(inorder, root.val) +1;
        //var rightlimit = GetIndex(preorder,  preorder[rightIndex]);
        return null;
      }
    }
}

